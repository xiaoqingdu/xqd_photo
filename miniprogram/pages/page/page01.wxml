<swiper indicator-dots='{{true}}' circular='{{true}}' autoplay='true'>
  <swiper-item wx:for="{{imgList}}" wx:key="{{index}}">
    <image src='{{item}}' mode='widthFix'></image>
  </swiper-item>
</swiper>
<button bindtap="addImg" type="primary" class="address-add">添加</button>